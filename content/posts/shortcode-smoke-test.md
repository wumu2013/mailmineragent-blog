---
title: "Shortcode Smoke Test (Draft)"
date: 2026-06-11
draft: true
description: "Internal smoke test page for the new SEO/GEO shortcode suite. Not for production."
faq:
  - q: "Mode A — what drives the visible HTML?"
    a: "The frontmatter `faq:` field on this page. The shortcode call below is paired with self-closing form, but Hugo treats it as a paired shortcode with empty Inner; the frontmatter data wins for rendering."
  - q: "Mode A — where does the JSON-LD come from?"
    a: "From the same frontmatter `faq:` field, emitted by `extend_head.html` which auto-detects the field via .Params.faq."
---

> This page is a build-time test harness for the `tldr`, `faq`, `eeat`, and `schema` shortcodes. It must remain `draft: true` so it never reaches `public/`.

## Test 1: `tldr` shortcode

{{< tldr >}}
A two-to-three sentence summary with **bold number** like 30-50 orders/day. Tests the aside block + markdownify + role="note" semantics.
{{< /tldr >}}

## Test 2: `faq` shortcode mode A (frontmatter-driven, no inline params)

The frontmatter `faq:` field on this page has TWO questions (intentional mismatch with Test 3's single inline question) — the frontmatter form is what drives mode A.

Note: `{{< faq />}}` self-closing would fail with the current template (the
`faq` shortcode consumes `.Inner` to enable paired-shortcode parsing). In
mode A, the JSON-LD is emitted by `extend_head.html` from frontmatter, not
by the shortcode; the shortcode call here is for visible HTML output.

{{< faq questions="" answers="" />}}

## Test 3: `faq` shortcode mode B (inline-driven) — frontmatter `faq:` is set, so JSON-LD is sourced from frontmatter (per design)

{{< faq
    questions="Mode B inlined — does the visible HTML still render from inline params?"
    answers="Yes. When the shortcode receives inline questions/answers params, those win for the visible HTML even if frontmatter faq: is also set. The JSON-LD, however, is sourced from frontmatter (extend_head.html priority: frontmatter > scratch)."
/>}}

## Test 4: `eeat` shortcode (full param set, with valid past-coverage paths)

Note: Hugo shortcode params are single-line. The `past` param below uses an HTML-like `\n` literal, parsed by the shortcode template's `split "\n"` logic.

{{< eeat
    focus="smoke testing the shortcode suite, validating JSON-LD emission, and protecting against typos in past-coverage links"
    past="/posts/from-tiktok-to-shopify-spanish-ecommerce/|Spanish TikTok-to-Shopify||/posts/amazon-refined-selection-90-percent-success-framework/|Amazon refined-selection 90% framework"
    disclosure="This is a test page, not a real article — all figures here are placeholders generated by the smoke test harness."
    cta="Spotted a bug in the shortcode suite?"
/>}}

## Test 5: `eeat` shortcode (with intentionally BAD past-coverage path — should warn, not error)

{{< eeat
    focus="triggering the warnf path for unresolvable past-coverage links"
    past="/posts/this-page-does-not-exist/|Ghost link"
    disclosure="(none - focus-only test)"
/>}}

## Test 6: `schema` shortcode — BreadcrumbList (inline JSON-LD only)

{{< schema type="BreadcrumbList"
    items="Home|/|Posts|/posts/|Shortcode smoke test|/shortcode-smoke-test/"
/>}}

## Test 7: `schema` shortcode — Article

{{< schema type="Article"
    headline="Shortcode Smoke Test (Draft)"
    author="MailMiner Editorial Team"
    datePublished="2026-06-11"
    description="Internal test page"
/>}}

## Test 8: `schema` shortcode — HowTo

{{< schema type="HowTo"
    name="How to verify the shortcode suite"
    description="Three steps to confirm the SEO/GEO shortcode build artifacts are byte-stable."
    steps="Build the site with hugo --gc --minify|Inspect public/posts/shortcode-smoke-test/index.html for JSON-LD blocks|Run a JSON-LD validator on each block"
/>}}

## Test 9: `schema` shortcode — Organization

{{< schema type="Organization"
    name="MailMiner Agent"
    url="https://mailminer.work"
/>}}

## Test 10: `schema` shortcode — FAQPage (alternative to `faq` shortcode for head-only injection)

{{< schema type="FAQPage"
    questions="Is this a separate JSON-LD block from the visible faq block?|When would I use schema type=FAQPage alone?"
    answers="Yes — they are independent output. Use one, not both, on the same page.|Use schema alone when you only want the JSON-LD (e.g. head injection) without rendering the visible HTML block."
/>}}

## Test 11: `schema` shortcode with unsupported type — must error (DISABLED)

Disabled: this shortcode uses `errorf` and would fail the build. To verify the error path, temporarily uncomment the line below and run `hugo --gc`.

```
{{</* schema type="CheeseCake" */>}}
```

## Test 12: `faq` shortcode mode B with mismatched q/a counts — must error (DISABLED)

Disabled: same reason as Test 11.

```
{{</* faq
    questions="Only one question?"
    answers="First answer.|Second answer that has no matching question."
*/>}}
```
